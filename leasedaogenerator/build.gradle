apply plugin: 'application'
apply plugin: 'java'

mainClassName = "com.hdu.greendao.LeaseDaoGenerator"

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile files('libs/freemarker-2.3.23.jar')
}